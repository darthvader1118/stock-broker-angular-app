import { NgInitDirectiveDirective } from './ng-init-directive.directive';

describe('NgInitDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NgInitDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
